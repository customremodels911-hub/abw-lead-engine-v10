# Tests

## Test 1 - Add, plan, retain, and prioritize a real opportunity [sanity]
Viewport: desktop
Covers: manual lead intake, POST /api/leads, GET /api/leads, PUT /api/leads/:id/status, PUT /api/leads/:id/operator, database persistence, cash-conversion prioritization, weighted pipeline metrics, estimated-value tracking, next-action persistence, conversion-stage persistence, priority queue
Description: Verifies a real opportunity can be created, valued, assigned a next action, advanced in stage and retained as an operator-ready pipeline record.

Steps:
1. Add lead "Priority Test" with phone "7575550101", email "priority@example.com", market "Virginia", project "Electrical", and estimated value 7500
2. In its Lead Inbox row change status to "Qualified", set Next Action to "Call customer for site visit", and set a future Due date
3. Refresh the page and inspect Today's Priority Queue, the row, and pipeline statistics

Expected: "Priority Test" remains visible with value 7500, status "Qualified", the saved next action and due date, appears in Today's Priority Queue ahead of lower-stage opportunities, and both Active pipeline $ and Weighted pipeline $ reflect its value.

## Test 2 - Scan public homeowner opportunities
Viewport: desktop
Covers: direct public opportunity workflow, POST /api/public-opportunities/scan, public Reddit ingestion, lead deduplication, GET /api/leads
Description: Verifies the operator can trigger a direct scan for public Charlotte, Hampton Roads / Virginia Beach, and New York City project requests and gets a visible result without disturbing the existing pipeline.

Steps:
1. Click "Scan Public Opportunities Now"
2. Inspect the result notice
3. Inspect the Lead Inbox

Expected: A notice beginning "Public scan complete:" is visible and any newly matched records appear with a public Reddit source for Charlotte, Hampton Roads / Virginia Beach, or New York City.

## Test 3 - Reject an unnamed manual lead
Viewport: mobile
Covers: manual intake validation, invalid-write guardrail
Description: Verifies the form blocks an empty lead name before creating a record.

Steps:
1. Leave Name blank
2. Click "Add to Pipeline"
3. Inspect the message above the form

Expected: "Lead name is required." is visible and no unnamed lead is added.

## Test 4 - Create synthetic marketplace leads without inflating revenue metrics
Viewport: desktop
Covers: Thumbtack test intake, Google Ads test intake, POST /api/test-thumbtack, POST /api/test-google-ads, GET /api/leads, database persistence, synthetic-lead separation
Description: Verifies marketplace test paths create visible records while remaining excluded from real-opportunity and revenue statistics.

Steps:
1. Click "Run Thumbtack Test"
2. Click "Run Google Ads Test"
3. Inspect the Lead Inbox and revenue statistics

Expected: "Thumbtack Test Lead" is visible with source "Thumbtack Test" and "Google Ads Test Lead" is visible with source "Google Ads Test", while neither increases Open real opportunities or pipeline-dollar metrics.

## Test 5 - Show webhook integration readiness
Viewport: desktop
Covers: GET /api/integrations/thumbtack/status, GET /api/integrations/google-ads/status, secure webhook readiness, Google Ads webhook integration route family
Description: Verifies the UI reports readiness for both external lead integrations.

Steps:
1. Open the application
2. Inspect the Thumbtack and Google Ads status badges
3. Inspect the lead dashboard

Expected: Both badges show either "READY" or "NEEDS SECRET", and the dashboard remains usable regardless of secret readiness.